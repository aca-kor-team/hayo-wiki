# 이미지 · GIF 넣는 법

## 1. 어디에 넣나요

`docs/assets/` 아래 주제별 폴더에 넣어주세요.

```
docs/assets/
├─ home/      ← 홈 화면용 (서버 대문 스크린샷, 배너 등)
├─ start/     ← 접속 방법, 첫날 안내용
├─ cooking/   ← 요리 시스템 (조리 장면 GIF, 레시피북 등)
├─ items/     ← 아이템 아이콘, 귀환석 사용 GIF
└─ ui/        ← GUI 스크린샷 (레시피 뷰어, 육절기 창 등)
```

폴더가 애매하면 아무 데나 넣고 알려주셔도 됩니다. 제가 정리해서 배치하겠습니다.

## 2. 파일 이름

한글도 되지만 **영문 소문자 + 하이픈**을 권합니다. 주소에 깨져 들어가는 일이 없습니다.

```
recipe-book-use.gif      (O)
meatcutter-gui.png       (O)
레시피북 사용.gif         (△ 동작은 하지만 주소가 지저분해집니다)
```

## 3. 권장 사양

| 종류 | 권장 | 이유 |
|---|---|---|
| 스크린샷 | PNG, 가로 1280~1920px | 글씨가 뭉개지지 않습니다 |
| GIF | 가로 640~960px, 5초 이내, 3MB 이하 | 크면 모바일에서 로딩이 답답합니다 |
| 아이콘 | PNG, 64~128px, 배경 투명 | 표 안에 넣기 좋습니다 |

!!! tip "GIF 가 너무 크다면"
    용량이 큰 GIF 는 **MP4 로 바꾸는 편**이 훨씬 낫습니다 (같은 화질에 용량 1/10).
    파일만 주시면 제가 변환해서 자동재생·무한반복으로 넣어드리겠습니다.

## 4. 넣고 나서

파일을 넣으신 뒤 **"넣었어"** 라고만 알려주세요.
폴더를 훑어서 각 페이지 알맞은 위치에 배치하고, 캡션과 대체 텍스트까지 채워 넣겠습니다.

---

### 참고 — 직접 넣고 싶으실 때의 문법

```markdown
![육절기로 고기를 손질하는 모습](../assets/cooking/meatcutter.gif)
```

- 대괄호 안은 **대체 텍스트**입니다. 이미지 아래 캡션으로도 자동 표시되고,
  이미지가 안 뜰 때 대신 보이며, 검색엔진도 이 글을 읽습니다. 꼭 채워주세요.
- 경로 앞의 `../` 개수는 페이지 위치에 따라 다릅니다.
  (`docs/index.md` → `assets/...`, `docs/cooking/recipes.md` → `../assets/...`)
- 클릭하면 확대되는 기능은 이미 켜져 있어서 따로 할 일이 없습니다.

너비를 줄이고 싶을 때:

```markdown
![레시피북](../assets/items/recipe-book.png){ width="320" }
```
